import { makeStyles } from "@mui/styles";

export const COMPONENT_NAME = "SpreadsheetPage";

export default makeStyles(
  (theme: any) => ({
    root: {
      marginTop: theme.spacing(4.25),
    },
  }),
  { name: COMPONENT_NAME }
);
